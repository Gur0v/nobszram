# nobszram config - /etc/nobszram/nobszram.conf

# enable/disable zram: true, false, 1, 0
enabled=true

# zram size: 25% (percent of RAM) or absolute like 1G, 512M
size=25%

# compression: lzo, lz4, zstd, lzo-rle
algorithm=zstd

# swap priority: -1 to 32767 (higher = preferred)
priority=100
